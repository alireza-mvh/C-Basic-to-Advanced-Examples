#include <stdio.h>
#include <conio.h>
#include <stdlib.h>

void main() {
  int count, numOfChars;
  for (count = 0;(getche())!= '.'; count++);
  printf("\nlength of the sentence is %d characters", count );

}